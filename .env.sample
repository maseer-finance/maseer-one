ETH_RPC_URL=
ETH_FROM=
ETH_KEYSTORE=
ETHERSCAN_API_KEY=
# Optional: pin `make test` to a fixed fork block to reuse Foundry's RPC cache
# and avoid HTTP 429 rate-limiting. Leave empty to fork `latest`.
ETH_FORK_BLOCK=
